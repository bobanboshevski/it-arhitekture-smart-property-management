name: Full System CI

on:
  workflow_dispatch:

jobs:
  call-property:
    uses: ./.github/workflows/property-service.yml

  call-booking:
    uses: ./.github/workflows/booking-analytics.yml

  call-pricing:
    uses: ./.github/workflows/dynamic-pricing.yml